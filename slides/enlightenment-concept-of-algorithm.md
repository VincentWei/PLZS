# 信息学启蒙<br/>第二单元 算法的概念

1. 理解算法的概念
1. 用流程图描述算法
1. 简化算法的描述
1. 用伪代码描述算法
1. 进制转换算法
1. 乘法算法
1. 求幂算法
1. 阶乘算法
1. 除法算法
1. 取模算法

		
## 第一讲

- 标题：理解算法的概念
- 旁白：清华大大，您老说算法、算法，算法到底是个啥？
- 主播：这个问题好！要说明白这个问题，咱先玩个数数游戏。
- 讲解要点：
   1. 算法的基本概念：把大象装进冰箱要几个步骤？
   1. 介绍数数游戏。
   1. 人类版并不适合计算机处理。
   1. 代数版谈什么是机械计算。
   1. 机械版谈要使用基本的算术运算，只要交给会算术的孩子就能得到正确结果。
   1. 终极版谈算法要严谨，没有漏洞。

	
### 理解算法的概念

<br/>
<br/>
<br/>
<br/>
<br/>
<br/>
<br/>


	
### 把大象装进冰箱要几个步骤？

1. 打开冰箱门。<!-- .element: class="fragment fade-in" -->
1. 把大象塞进冰箱。<!-- .element: class="fragment fade-in" -->
1. 关上冰箱门。<!-- .element: class="fragment fade-in" -->

	
### 数数游戏<!-- .element: class="fragment fade-out" -->

<br/>
<br/>
<br/>
<br/>
<br/>
<br/>
<br/>

	
### 数数游戏必胜算法（人类版）

1. 判断给定的数是否可以被 7 整除。如果能，则拍手；如果不能，继续下一步的处理。
1. 判断给定的数的每一位是否包含 7。如果有任何一位是 7，则拍手，否则说出这个数。

	
### 数数游戏必胜算法（代数版）

1. 设给定的数为 `x`。
1. 计算 `x` 被 `7` 除的余数 `r`。如果 `r` 等于 `0`，则拍手；如果不等于 `0`，进入下一步处理。
1. 按十进制分解 `x` 的个位、十位、百位等数字，如果任意一位是 `7`，则拍手，否则说出这个数。

	
### 数数游戏必胜算法（机械版）

1. 设给定的数为 `x`。
1. 计算 `x` 被 `7` 除的余数 `r`。如果 `r` 等于 `0`，则拍手，算法结束；如果不等于 `0`，则进入第 3 步处理。
1. 计算 `x` 除以 `10` 的余数 `r`，并令 `x` 等于 `x` 除以 `10` 的商。
1. 如果 `r` 等于 `7`，则拍手，算法结束；否则，转入第 5 步。
1. 如果 `x` 等于 `0`，则说出 `x`，算法结束；否则，转入第 3 步。

	
### 数数游戏必胜算法（终极版本）

1. 设给定的数为 `x`，并令 `y` 等于 `x`。
1. 计算 `y` 被 `7` 除的余数 `r`。如果 `r` 等于 `0`，则拍手，算法结束；如果不等于 `0`，则进入第 3 步处理。
1. 计算 `y` 除以 `10` 的余数 `r`，并令 `y` 等于 `y` 除以 `10` 的商。
1. 如果 `r` 等于 `7`，则拍手，算法结束；否则，转入第 5 步。
1. 如果 `y` 等于 `0`，则说出 `x`，算法结束；否则，转入第 3 步。

	
### 作业

<div class="no-list-style">

- 1) 从 `2`、`3`、`5`、`8` 中选出满足如下代数式时 `a` 的可能取值（第一行为示例解答）：

`$$
\begin{align}
    a + 3 &\lt 10:  a = 2,\ 3,\ 5  \\
    a - 3 &= 5:         \\
    a &\gt 3:           \\
    a &\ge 3:           \\
    2a & = 6:           \\
    2a + 1 & = 5:       \\
\end{align}
$$`

- 2) 用文字描述进制转换算法。

</div>

		
## 第二讲

- 标题：用流程图描述算法
- 主播：兮兮，今天教你用流程图描写一个算法。
- 旁白：好的。
- 讲解要点：
   1. 回顾文字描述的算法，提出问题：不直观。
   1. 常用流程图图例：开始、结束、判定、循环、输入、输出。
   1. 绘制数数游戏必胜算法的流程图。

	
### 用流程图描述算法

<br/>
<br/>
<br/>
<br/>
<br/>
<br/>
<br/>

	
### 回顾数数游戏必胜算法的文字描述

1. 设给定的数为 `x`，并令 `y` 等于 `x`。
1. 计算 `y` 被 `7` 除的余数 `r`。如果 `r` 等于 `0`，则拍手，算法结束；如果不等于 `0`，则进入第 3 步处理。
1. 计算 `y` 除以 `10` 的余数 `r`，并令 `y` 等于 `y` 除以 `10` 的商。
1. 如果 `r` 等于 `7`，则拍手，算法结束；否则，转入第 5 步。
1. 如果 `y` 等于 `0`，则说出 `x`，算法结束；否则，转入第 3 步。

	
### 常用流程图图例<!-- .element: class="fragment fade-out" -->

	
### 数数游戏必胜算法的流程图<!-- .element: class="fragment fade-out" -->

	
### 作业

<div class="no-list-style">

- 1) 从 `2`、`3`、`5`、`8` 中选出满足下面所列逻辑关系的一组数值（第一行为示例解答）：

`$$
\begin{align}
    a &= b:   \quad a = 2,\ b = 2    \\
    a - 3 &\gt b:                    \\
    2a &\gt b:                       \\
    2^a &\ge b:                      \\
    a^2 &\lt b:                      \\
    a^3 &\le b:                      \\
\end{align}
$$`

- 2) 用流程图描述进制转换算法。

</div>

		
## 第三讲

- 标题：简化算法的描述
- 旁白：清华大大，不管用文字还是流程图，每次写好多字，我的手都快抽筋了。
- 主播：哎呀，这可不好，是时候教你如何简化描述了。
- 要点：
   1. 运算符的概念
   1. 基本算术运算符
   1. 赋值运算符
   1. 基本逻辑判断运算符
   1. 用运算符简化数数游戏必胜算法的流程图

	
### 简化算法的描述

<br/>
<br/>
<br/>
<br/>
<br/>
<br/>
<br/>

	
### 运算符的概念<!-- .element: class="fragment fade-out" -->

<br/>
<br/>
<br/>
<br/>
<br/>
<br/>
<br/>


	
### 基本算术运算符

<div class="narrow-table">

| 名称        | 运算符  | 示例        | 结果  |
| ---         | ---     | ---         | ---   |
| 加法        | `+`     | `3 + 5`     | `8`   |
| 减法        | `-`     | `5 - 3`     | `2`   |
| 乘法        | `*`     | `5 * 3`     | `15`  |
| 除法        | `/`     | `5 / 2`     | `2.5` |
| 整除        | `//`    | `5 // 2`    | `2`   |
| 取模        | `%`     | `5 % 2`     | `1`   |

</div>

	
### 赋值运算符

<div class="narrow-table">

| 名称        | 运算符  | 示例        | 结果            |
| ---         | ---     | ---         | ---             |
| 赋值        | `:=`    | `x := 3`    | `x` 的值为 3    |

</div>

	
### 基本逻辑判断运算符

<div class="narrow-table">

| 名称        | 运算符  | 示例        | 结果    |
| ---         | ---     | ---         | ---     |
| 等于        | `==`    | `3 == 5`    | `假`    |
| 大于        | `>`     | `5 > 3`     | `真`    |
| 小于        | `<`     | `5 < 3`     | `假`    |
| 大于等于    | `>=`    | `5 >= 3`    | `真`    |
| 小于等于    | `<=`    | `5 <= 3`    | `假`    |
| 不等于      | `!=`    | `5 != 3`    | `真`    |

</div>

	
### 用运算符简化数数游戏必胜算法的流程图<!-- .element: class="fragment fade-out" -->

	
### 作业

<div class="no-list-style">

- 1) 如 `3 + 5` 或者 `5 > 3` 这样通过运算符组织起来的算式或代数式在计算机中称为表达式。当 `a` 分别取 `4`、`9`、`16` 时，计算对应表达式的值（第二行为示例解答）：

`$$
\begin{align}
    a           &:  \quad && 4 \quad && 9  \quad && 16    \\
    3 + a       &:  \quad && 7 \quad && 12 \quad && 19    \\
    2 * a + 1   &:                                        \\
    100 - 3 * a &:                                        \\
    a \ // \ 3  &:                                        \\
    a \ \% \ 3  &:                                        \\
    a \gt 3     &:                                        \\
    a \ge 8     &:                                        \\
    2a == 8     &:                                        \\
\end{align}
$$`

- 2) 用学到的运算符重画进制转换算法的流程图。

</div>

		
## 第四讲

- 标题：用伪代码描述算法
- 主播：兮兮，流程图虽然直观，但画起来麻烦；今天教你用伪代码描写一个算法。
- 旁白：好的呀。
- 要点：
   1. 有固定的格式要求和预先定义的关键词
   1. 解释关键词、代码、代码行等的概念
   1. 通过缩进表示代码行的嵌套关系

	
### 用伪代码描述算法

<br/>
<br/>
<br/>
<br/>
<br/>
<br/>
<br/>

	
### 用伪代码描述数数游戏的必胜算法

```mathematica
算法开始 数数游戏必胜 (x)
    如果开始 (当 x % 7 == 0 时)
        执行 拍手
        终止
    否则

        y := x
        循环开始 (当 y != 0 时)
            y := y // 10
            r := y % 10
            如果开始 (当 r == 7 时)
                执行 拍手
                算法结束
            如果结束
        循环结束

        执行 说出 x
    如果结束
算法结束
```

	

```mathematica
ALGO countingGame (x)
    IF (x % 7 == 0)
        EXECUTE clapHands
        STOP
    ELSE

        y := x
        WHILE (y != 0)
            y := y // 10
            r := y % 10
            IF (r == 7)
                EXECUTE clapHands
                STOP
            ENDIF
        ENDWHILE

        EXECUTE speak x
    ENDIF
ENDALGO
```

	
### 用考鼎码描述数数游戏必胜算法

```mathematica
算始 数数游戏必胜 (x)
    若始 (x % 7 == 0)
        执行 拍手
        终止
    若否
        y := x
        当始 (y != 0)
            y := y // 10
            r := y % 10
            若始 (r == 7)
                执行 拍手
                终止
            若终
        当终

        执行 说出 x
    若终
算终
```

	
### 考鼎码关键词

<div class="narrow-table">

| 关键词      |  解释                   | 示例用法                      |
| ---         |  ---                    | ---                           |
| `算始/算终` |  定义一个算法的开始/终止|                               |
| `若始/若终` |  定义一个判断的开始/结束|                               |
| `又若`      |  定义一个判断的其他条件 | `又若 (x == 0)`               |
| `若否`      |  定义一个判断的否则条件 |                               |
| `当始/当终` |  定义一个循环的开始/结束|                               |
| `输入`      |  读取用户输入           | `x := 输入`                   |
| `输出`      |  输出变量或者数据       | `输出 x`                      |
| `返回`      |  返回一项数据           | `返回 0`                      |
| `终止`      |  终止算法执行           | `终止`                        |
| `执行`      |  执行一个动作           | `执行 拍手`                   |
| `真`        |  逻辑真值               | `返回 真`                     |
| `假`        |  逻辑假值               | `返回 假`                     |
| `未定义`    |  未定义                 | `返回 未定义`                 |

</div>


	
### 作业

<div class="no-list-style">

- 1) 下面的考鼎码计算一个数的平方，请找出其中的错误写法（给出行号和错误原因）。

```mathematica []
算始 平方 (n)
    若始 n = 0
    返回 0
    又若 n = 1
        返回 1
    若否
    返回 n * n
算终
```

- 2) 为算法提供的计算用数值称为参数，在考鼎码中可以用单个字母或者更有意义的单词或其缩写为参数取名。这点和代数式不同，代数式通常用单个字母或者单个字母加上下标的方式来表示不同的数值。请给出下面的考鼎码算法在参数 `height`（英文单词，表示高度）分别取 `0`、`50`、`100`、`200` 时的输出结果：

```mathematica
算始 限高 (height)
    若始 (height > 100)
        输出 100
    若否
        输出 height
    若终
算终
```

- 3) 用考鼎码描述进制转换算法。

</div>

		
## 第五讲

- 标题：进制转换算法
- 旁白：清华大大，能不能看看我用考鼎码写的进制转换算法？
- 主播：这算你的第一个程序啦，快给我看看。
- 要点：
   1. 分析兮兮的算法
   1. 序列及其操作
   1. 给出修改版

	
### 进制转换算法（兮兮版）

```mathematica
算始 转换进制 (x, b)
    当始 (x != 0)
        x := x // b
        r := x % b
        输出 r
    当终
算终
```

	
### 序列及其操作

1. 序列的定义：一组有序的元素构成
1. 考鼎码中序列的基本操作

| 操作                          | 运算符  | 示例                    | 结果                          |
| ---                           | ---     | ---                     | ---                           |
| 定义空序列                    | `:=`    | `s := []`               | `s` 是一个空序列              |
| 串接两个序列                  | `+`     | `s := [0, 2] + [3, 4]`  | `s` 为 `[0, 2, 3, 4]`         |
| 获取序列中第 `i` 个元素       | `[i]`   | `a := s[0]`             | `a` 的值为 `0`                |
| 设置序列中第 `i` 个元素       | `[i]`   | `s[0] := 1`             | `s` 变为 `[1, 2, 3, 4]`       |
| 将某个元素插入到序列的前面    | `+`     | `s := [0] + s`          | `s` 变为 `[0, 1, 2, 3, 4]`    |
| 将某个元素追加到序列的前面    | `+`     | `s := s + [5]`          | `s` 变为 `[0, 1, 2, 3, 4, 5]` |

	
### 进制转换算法（修改版）

```mathematica
算始 转换进制 (x, b)
    若始 (b <= 1)
        返回 未定义
    若终

    s := []
    若始 (x = 0)
        s := [0]
        返回 s
    若终

    当始 (x != 0)
        r := x % b
        s := [r] + s
        x := x // b
    当终

    返回 s
算终
```

	
### 作业

<div class="no-list-style">

- 1) 找出下面的考鼎码中错误的用法（给出行号和错误原因）。

```mathematica []
算始 序列操作
    s = []
    s := s + 3
算终
```

- 2) 下面的考鼎码生成一个偶数序列，请给出当 `n` 取 `10` 时生成的序列。

```mathematica
算始 偶数序列 (n)
    s := []
    i := 0
    当始 (i < n)
        s := s + [i * 2]
        i := i + 1
    当终
    返回 s
算终
```

- 3) 用流程图描述本讲给出的进制转换算法之修改版。

</div>

		
## 第六讲

- 标题：用加法实现乘法的算法
- 旁白：清华大大，您看看我用加法计算乘法的算法对不对？
- 主播：哇，一发不可收拾了！快给我看看。
- 要点：
   1. 分析兮兮版。
   1. 给出修改版。

	
### 加法实现的乘法算法（兮兮版）

```mathematica
算始 乘法 (n, m)
    当始 (m != 0)
        n := n + n
        m := m - 1
    当终
    输出 n
算终
```

	
### 加法实现的乘法算法（修改版）

```mathematica
算始 乘法 (n, m)
    r := 0
    当始 (m != 0)
        r := r + n
        m := m - 1
    当终
    输出 r
算终
```

	
### 作业

<div class="no-list-style">

- 1) 在代数中，因为通常使用单个字母表示一个数值，所以两个数相乘时，会省略乘号，直接写成 `ab` 或者 `102a` 这样的形式。但在考鼎码中不能如此书写。请将下面的代数式转成对应的考鼎码表达式（第一行为示例解答）：

`$$
\begin{align}
    ab &:  a * b        \\
    2a + b&:             \\
    a^2 + 3b&:           \\
\end{align}
$$`

- 2) 使用本讲给出的乘法算法，当给定的初始值 `n` 及 `m` 取下列各值时，写出算法每一个步骤的变量值以及执行结果（可使用表格）：
     a) `n = 2, m = 0`<br/>
     b) `n = 11, m = 3`

</div>

		
## 第七讲

- 标题：求幂算法
- 旁白：清华大大，您看看我计算 x 的 n 次方的算法是否正确？
- 主播：哇，我都快应接不暇了！快给我看看。
- 要点：
   1. 分析兮兮版。
   1. 给出常见错误版。

	
### 求幂算法（兮兮版）

```mathematica
算始 求幂 (x, n)
    p := 1
    当始 (n != 0)
        p := x * p
        n := n - 1
    当终
    输出 p
算终
```

	
### 求幂算法（常见的错误版本）

```mathematica
算始 求幂 (x, n)
    当始 (n != 0)
        x := x * x
        n := n - 1
    当终
    输出 x
算终
```

	
### 作业

- 使用本讲给出的求幂算法，当给定的初始值 `x` 及 `n` 取下列各值时，写出算法每一个步骤的变量值以及执行结果（可使用表格）
   1. `x = 3, n = 0`
   1. `x = 2, n = 4`

		
## 第八讲

- 标题：阶乘算法
- 旁白：清华大大，您看看我计算阶乘的算法是否正确？
- 主播：哇，都知道阶乘了哇！快给我看看。
- 要点：
   1. 解释阶乘。
   1. 分析兮兮版。
   1. 给出修改版。

	
### 阶乘算法（兮兮版）

```mathematica
算始 阶乘 (x, n)
    r := n
    当始 (n > 1)
        r := r * (n - 1)
        n := n - 1
    当终
    输出 r
算终
```

	
### 阶乘算法（修改版）

```mathematica
算始 阶乘 (n)
    若始 (n == 0)
        输出 1
        终止
    若终

    r := 1
    i := 2
    当始 (i <= n)
        r := r * i
        i := i + 1
    当终
    输出 r
算终
```

	
### 作业

- 使用本讲给出的阶乘算法，当给定的初始值 `n` 取下列各值时，写出算法每一个步骤各变量的值以及执行结果（可使用表格）
   1. `n=0`
   1. `n=1`
   1. `n=5`

		
## 第九讲

- 标题：减法实现除法
- 旁白：清华大大，既然用加法可以实现乘法，我试着用减法实现了除法，您给看看。
- 主播：兮兮好聪明，快给我看看你的考鼎码。
- 要点：
   1. 分析兮兮的版本
   1. 给出优化版本
   1. 给出流程图

	
### 用减法实现除法的考鼎码（兮兮版）

```mathematica
算始 除法 (a, b)
    若始 (b == 0)
        输出 未定义
        终止
    若终
    若始 (a < b)
        输出 0
        终止
    若终
    若始 (a == b)
        输出 1
        终止
    若终
    q := 0
    当始 (a > b)
        q := q + 1
        a := a - b
    当终
    输出 q
算终
```

	
### 用减法实现除法的考鼎码（优化版）

```mathematica
算始 除法 (a, b)
    若始 (b == 0)
        输出 未定义
        终止
    若终

    q := 0
    当始 (a >= b)
        q := q + 1
        a := a - b
    当终
    输出 q
算终
```

	
### 作业

- 使用本讲给出的除法算法，当给定的初始值 `a, b` 取下列各值时，写出算法每一个步骤各变量的值以及执行结果（可使用表格）
   1. `a=0, b=0`
   1. `a=0, b=1`
   1. `a=2, b=3`
   1. `a=3, b=2`

		
## 第十讲

- 标题：减法实现取模
- 旁白：清华大大，我用减法了实现了取模运算，我觉得接近完美，您看看是不是？
- 主播：要谦虚哦，完美的代码很少见呢。
- 要点：
   1. 分析兮兮的版本
   1. 思考负数的情况

	
### 用减法实现取模运算（兮兮版）

```mathematica
算始 取模 (a, b)
    若始 (b == 0)
        输出 未定义
        终止
    若终

    当始 (a >= b)
        a := a - b
    当终
    输出 a
算终
```

	
### 思考

- 当被除数或除数是负数时，取模运算应该如何改进？

	
### 作业

- 使用本讲给出的取模算法，当给定的初始值 `a, b` 取下列各值时，写出算法每一个步骤各变量的值以及执行结果（可使用表格）
   1. `a=0, b=0`
   1. `a=0, b=1`
   1. `a=2, b=3`
   1. `a=3, b=2`

